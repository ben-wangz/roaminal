export function SystemStatus({ connected, hostname, sessionCount, latencyMs, persistenceDegraded }: { connected: boolean; hostname: string; sessionCount: number; latencyMs: number | null; persistenceDegraded: boolean }) { return <div className="system-status"><span className={`connection-dot ${connected ? 'online' : 'offline'}`} /> <span>{connected ? 'Connected' : 'Reconnecting'}</span><span className="status-host">{hostname || 'Roaminal'} · {sessionCount}{latencyMs === null ? '' : ` · ${latencyMs}ms`}</span>{persistenceDegraded && <span className="status-warning" role="status">Persistence degraded</span>}</div>; }
