export function SystemStatus({ connected, hostname, sessionCount }: { connected: boolean; hostname: string; sessionCount: number }) { return <div className="system-status"><span className={`connection-dot ${connected ? 'online' : 'offline'}`} /> <span>{connected ? 'Connected' : 'Reconnecting'}</span><span className="status-host">{hostname || 'Roaminal'} · {sessionCount}</span></div>; }
