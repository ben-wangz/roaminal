import type { SessionSummary } from '../terminal/terminal-protocol';

export function Sidebar({ sessions, active, open, onToggle, onSelect, onClose, onCreate }: { sessions: SessionSummary[]; active: string | null; open: boolean; onToggle: () => void; onSelect: (id: string) => void; onClose: (id: string) => void; onCreate: () => void }) { return <aside className={`sidebar ${open ? 'open' : ''}`}><div className="sidebar-header"><div className="brand-mark small">r<span>&gt;</span></div><strong>Roaminal</strong><button className="icon-button sidebar-toggle" onClick={onToggle} aria-label="Toggle sidebar">{open ? '‹' : '›'}</button></div><div className="sidebar-actions"><button className="primary full" onClick={onCreate}>+ New terminal</button></div><div className="session-list">{sessions.map((session) => <div className={`session-row ${session.id === active ? 'active' : ''}`} key={session.id}><button onClick={() => onSelect(session.id)}><span className="session-indicator" /> <span><b>{session.title || 'Terminal'}</b><small>{session.cwd}</small></span></button><button className="row-close" onClick={() => onClose(session.id)} aria-label="Close terminal">×</button></div>)}</div><div className="sidebar-footer">Single instance · Bash</div></aside>; }
